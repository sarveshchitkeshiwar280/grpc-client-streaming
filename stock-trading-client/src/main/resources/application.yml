grpc:
  client:
    stockService:
      address: ${GRPC_SERVER_ADDRESS:static://stock-trading-server:6565}
      negotiationType: PLAINTEXT
      enable-keep-alive: true
      keep-alive-without-calls: true

logging:
  level:
    io.grpc: ${LOG_LEVEL_GRPC:INFO}